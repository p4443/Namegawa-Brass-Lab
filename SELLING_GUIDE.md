# 買い切り版 販売・運用手順

## ライセンス仕様

- 1ライセンスキーにつき1社だけ登録できます。
- 月額課金、更新課金、有効期限はありません。
- ライセンスキーは初回アカウント登録専用です。一度登録に使用したキーを別アカウントの登録へ再利用することはできませんが、登録済みユーザーはメールアドレスとパスワードで何度でもログインできます。
- 登録後はメールアドレスとパスワードで通常ログインします。
- 既に登録済みの会社へ後からライセンス入力を求めることはありません。

## 決済から自動納品まで

1. Stripe Payment LinkをSNS、メール、QRコードなどで案内します。
2. 個人版500円または会社版50,000円の決済が完了します。
3. Stripe Webhookが未使用ライセンスキーを1件だけ自動発行します。
4. Resendが購入者へ本番URL、ライセンスキー、Stripe領収書リンクを送信します。
5. 購入者は「アカウントを新規登録」でキーを一度だけ使用します。

購入者へソースコード、Dockerイメージ、SQLite DB、サーバー認証情報は渡しません。アプリの修正とデプロイは販売者だけが行います。

StripeとRenderの設定は [STRIPE_SALES_SETUP.md](STRIPE_SALES_SETUP.md) に従います。返金・チャージバック後に登録済みアカウントを停止する処理は自動化されていないため、販売者が個別に管理してください。

## 手動ライセンスキーの発行

本番のSQLite DBを接続したコンテナで実行します。

```sh
docker run --rm \
  -e DATABASE_FILE=/data/tennko.sqlite \
  -v tennko-kakuninnbo-data:/data \
  tennko-kakuninnbo:latest \
  npm run --silent license:create
```

複数発行する場合は末尾に発行数を指定します。

```sh
npm run --silent license:create -- 10
```

表示されたキーは購入者へ安全に送付してください。DBにはキーのハッシュだけが保存されるため、後からキー全文を再表示することはできません。

## 本番起動

Renderでの初回公開と更新は [RENDER_DEPLOYMENT.md](RENDER_DEPLOYMENT.md) に従います。RenderのWeb Serviceと永続ディスクには継続費用が発生するため、買い切り価格へ想定運用期間の費用を含めてください。

ローカルまたは別サーバーで起動する場合は次の構成を使用します。

```sh
docker run -d \
  --name tennko-kakuninnbo \
  -p 3000:3000 \
  -e NODE_ENV=production \
  -e DATABASE_FILE=/data/tennko.sqlite \
  -v tennko-kakuninnbo-data:/data \
  tennko-kakuninnbo:latest
```

本番ではHTTPS対応のドメインを使用してください。`NODE_ENV=production` ではログインCookieに `Secure` 属性が付きます。

## バックアップ

アプリは起動時と24時間ごとに `/data/backups` へ整合性確認済みバックアップを作成し、新しい14件を保持します。会社、利用者、セッション、点呼記録、添付ファイル、ライセンス利用状態が含まれます。月1回以上、最新バックアップをRender外の暗号化された保管先へ移してください。

## 障害対応

1. `/api/health`、Renderのサービス状態、ログ、永続ディスク容量を確認します。
2. 影響範囲、発生時刻、直前のデプロイまたは設定変更を記録します。
3. 必要に応じてRenderへ照会し、再起動、設定修正または最新の整合性確認済みバックアップから復元します。
4. 購入者へ、判明している影響、代替手段、対応状況を案内します。確定していない復旧時刻は約束しません。
5. 復旧後、ログイン、記録、添付、月次出力を確認し、データ欠損の可能性と対象期間を購入者へ案内します。
6. 原因と再発防止策を記録し、監視、容量、バックアップまたは運用手順を見直します。

バックアップから復元する前に、障害時点のDBとログを可能な範囲で保全してください。正常な現行データを誤って古いバックアップで上書きしないよう、復元対象と時刻を確認します。

## 納品物

- 本番HTTPS URL
- 未使用のライセンスキー
- `BUYER_GUIDE.md` のPDFまたは閲覧用ページ
- 確定済みの利用規約とプライバシーポリシー

秘密情報はパスワード管理ツールで管理し、ソースコード、SQLite DB、販売済みキー、テストデータを購入者へ送付しないでください。

## セキュリティ方針

- ライセンス照合、会社登録、データ分離はサーバー側で実行します。
- 本番はHTTPSを必須とし、管理ポートとDBを公開しません。
- アプリのソースとDockerイメージへ購入者がアクセスできない権限構成にします。
- OS、Node.js、依存パッケージを定期的に更新します。
- SQLite DBを暗号化された保管先へ定期バックアップします。
- ログイン異常、容量、死活状態を監視します。

ブラウザーでは安定したHWIDを安全に取得できず、端末交換やプライバシー上の問題もあるため、端末固定は採用しません。ライセンスキーは購入した利用権を最初のアカウントへ登録するためにだけ使用し、登録後はユーザーのメールアドレスとパスワードによるサーバー側認証で利用範囲を管理します。
